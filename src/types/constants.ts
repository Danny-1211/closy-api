type Style = {
    styleId: string;
    styleName: string;
};

type Color = {
    colorId: string;
    colorName: string;
};

type Occasion = {
    occasionId: string;
    occasionName: string;
};

export { Style, Color, Occasion };