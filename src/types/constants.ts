type Style = {
  styleId: string;
  styleName: string;
};

type Color = {
  colorId: string;
  colorName: string;
};

type Occasion = {
  occasionId: string;
  occasionName: string;
};

type Season = {
  seasonId: string;
  seasonName: string;
}

type Category = {
  categoryId: string;
  categoryName: string;
}

type Brand = {
  brandName: string;
}


export { Style, Color, Occasion, Season, Category, Brand };
